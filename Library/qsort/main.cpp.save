#include <iostream>
#include <ctime>
#include <cstdlib>
using namespace std;
int n;
int list[1000];
void sort(int l, int r)
{
    int i, j;
    int tmp, mid;
    i = l;
    j = r;
    mid = list[l + rand() % (r - l + 1)];
    do
    {
        while (list[i] < mid)
            i++;
        whi
    }
    while (i <= j);
}
int main()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> list[i];
    srand(time(NULL));
    sort(1, n);
    system("Pause");
    return 0;
}